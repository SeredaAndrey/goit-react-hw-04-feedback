import React from 'react';
import { Message } from './nptification.styled';

const NotificationMessage = ({ message }) => {
  return <Message>{message}</Message>;
};

export default NotificationMessage;
